import { UiInput } from './input.directive';

describe('UiInput', () => {
  it('should create an instance', () => {
    // const directive = new UiInput();
    // expect(directive).toBeTruthy();
  });
});
